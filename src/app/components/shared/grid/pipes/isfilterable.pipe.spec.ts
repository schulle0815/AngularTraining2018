import { IsfilterablePipe } from './isfilterable.pipe';

describe('IsfilterablePipe', () => {
  it('create an instance', () => {
    const pipe = new IsfilterablePipe();
    expect(pipe).toBeTruthy();
  });
});
